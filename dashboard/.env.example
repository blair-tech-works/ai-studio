# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# The actual orchestrator API runs on localhost:3001
# But Next.js rewrites /api/* to http://localhost:3001/api/*
# So the frontend always uses /api routes
